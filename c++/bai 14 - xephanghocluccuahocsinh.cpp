#include <iostream>
using namespace std;
/*int main() {
	float dkt, dgk, dck;
	float dtb = 0;
	cout << "Nhap vao diem kiem tra: " << endl;
	cin >> dkt;
	cout << "Nhap vao diem giua ki: " << endl;
	cin >> dgk;
	cout << "Nhap vao diem cuoi ki: " << endl;
	cin >> dck;
	dtb = (dkt + dgk + dck) / 3;
	cout << "Diem trung binh la: " << dtb << endl;
	if (dtb >= 9.0) {
		cout << " Xep hang A." << endl;
	}
	else if (dtb >= 7.0 && dtb <= 9.0) {
		cout << " Xep hang B." << endl;
	}
	else if (dtb >= 5.0 && dtb <= 7.0) {
		cout << " Xep hang C." << endl;
	}
	else {
		cout << " Xep hang D." << endl;
	}
}*/