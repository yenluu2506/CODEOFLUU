#include <iostream>
#include <math.h>
using namespace std;
/*int main() {
	int a, b, c;
	cout << "Nhap vao 3 he so: ";
	cin >> a >> b >> c;
	cout << "Phuong trinh da nhap la: " << a << "x*x + " << b << "x + " << c << endl;
	if (a == 0 && b == 0) {
		cout << "Phuong trinh vo nghiem." << endl;
	}
	else if (a == 0) {
		cout << "Phuong trinh co 1 nghiem: " << -c / b << endl;
	}
	else {
		int detal;
		detal = b * b - 4 * a * c;
		if (detal < 0) {
			cout << "Phuong trinh vo nghiem." << endl;
		}
		else if (detal == 0) {
			cout << "Phuong trinh co nghiem kep : ";
			cout << " x1 = x2 = " << -b / (2 * a) << endl;
		}
		else {
			cout << "Phuong trinh co 2 nghiem phan biet: " << endl;
			cout << "x1 = " << (-b + sqrt(detal)) / (2 * a) << endl;
			cout << "x2 = " << (-b - sqrt(detal)) / (2 * a) << endl;
		}
	}
}*/