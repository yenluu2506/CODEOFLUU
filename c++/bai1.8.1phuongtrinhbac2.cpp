#include <iostream>
using namespace std;
/* int main() {

	int a, b, c;
	double x1 = 0, x2 = 0;
	
	cout << " nhap vao ba gia tri a, b, c: ";
	cin >> a >> b >> c;

	int dental = b * b - 4 * a / c;
	if (dental < 0) {
		cout << " PHUONG TRINH VO NGHIEM: " << endl;
	}
	else if (dental == 0) {
		x1 = x2 = -b / (2 * a);
		cout << " PHUONG TRINH CO NGHIEM KEP " << " a " << " x ^ 2" << " + " << " b " << " x " << " + " << " c " << " = " << " 0 la: " << x1 << x2 << endl;
	}
	else {
		x1 = (-b - sqrt(dental)) / (2 * a);
		x2 = (-b + sqrt(dental)) / (2 * a);
		cout << " PHUONG TRINH CO HAI NGHIEM PHAN BIET " << " a " << " x ^ 2 " << " + " << " b " << " x " << " c " << " = " << " 0 la: " << x1 << x2 << endl;
	}

	return 0;
	system("pause");
} */

// lỗi chờ sửa